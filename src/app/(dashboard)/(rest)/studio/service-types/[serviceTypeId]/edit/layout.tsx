import type { Metadata } from "next";

export const metadata: Metadata = { title: "Edit Service Type" };

export default function EditServiceTypeLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
