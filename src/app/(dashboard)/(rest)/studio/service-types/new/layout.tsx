import type { Metadata } from "next";

export const metadata: Metadata = { title: "New Service Type" };

export default function NewServiceTypeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
